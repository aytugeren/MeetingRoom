# Coturn TURN server config
# domain ve credential'ları kendi değerlerinle değiştir

listening-port=3478
tls-listening-port=5349

fingerprint
lt-cred-mech

# Domain ve kullanıcı — MUTLAKA değiştir
realm=yourdomain.com
user=meetingroom:GUCLU_SIFRE_KOY

# Dış IP — VPS'in public IP'si
# external-ip=1.2.3.4

# Relay port aralığı
min-port=49160
max-port=49200

# Log
log-file=/var/log/coturn/turnserver.log
